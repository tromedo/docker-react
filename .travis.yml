language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t tromedo/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true tromedo/docker-react npm run test